services:
  - type: web
    name: MTN-Clarity-AI-BE
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/app.js
    envVars:
      - key: NODE_ENV
        value: production
